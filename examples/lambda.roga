let double =
    x -> x * 2

let triple =
    x -> x * 3

let quadruple x =
    x * 4

let quadrupleAndAddDoubleOf x =
    y -> (quadruple y) + (double x)

let main =
    let
        _ = println (double (triple 3)),
        _ = println (quadrupleAndAddDoubleOf 3 2)
    in
        42
